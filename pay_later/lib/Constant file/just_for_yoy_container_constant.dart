import 'package:flutter/material.dart';

class JustForYoyContainerConstant extends StatelessWidget {
  const JustForYoyContainerConstant({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
